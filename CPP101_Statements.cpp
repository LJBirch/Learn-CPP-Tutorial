#include <iostream>

int main()
{
	std::cout << "Hello World!";
	return 0;
}

/*
	Quiz:

		Question #1 - What is a statement?
		Answer: A statement is an instruction in a computer program that tells the computer to perform an action.

		Question #2 - What is a function?
		Answer: A function is a collection of statements that exectues sequentially.

		Question #3 - What is the name of the function that all programs must have?
		Answer: A function called main, that returns an integer.

		Question #4 - When a program is run, where does execution start?
		Answer: Execution starts with the first statement inside the main function.

		Question #5 - What symbol are statements in C++ often ended with?
		Answer: ; (a semicolon)

		Question #6 - What is a syntax error?
		Answer: A syntax error is a compiler error that occurs at compile-time when your program violates the grammar rules of the C++ languages.

		Question #7 - What is the C++ standard library?
		Answer: A library is a collection of precompilied code that been "pacakged up" for reuse in other programes. The C++ standard library is a library that ships with C++. It contains additional functionality to use in your programs.
*/